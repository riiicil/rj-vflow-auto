# Agent Log — 2026-09-20

---

<!-- NEW ENTRIES GO HERE — newest at top, oldest at bottom -->

## Session Entry 4 — Model Defaults Realignment, Strict Mode Normalization, Sequential F2V Frame Ingestion, and Thumbnail Persistence Hydration

**Time:** ~11:15 WIB  
**Branch:** `task/dual-mode-ui`  
**Commit:** Pending (`fix(engine): model defaults realignment strict mode normalization sequential f2v frame injection and thumbnail hydration`)  
**Product code changed:** YES (`src/core/FlowStorage.js`, `src/core/QueueManager.js`, `src/overlay/FlowHUDHost.js`, `src/overlay/FlowHUDTemplates.js`, `src/services/FlowIngredientService.js`, `src/services/FlowSettingsService.js`)  
**Pushed:** NO  

### What Was Done
- **Issue 1: Model Defaults Realignment (`src/core/FlowStorage.js`, `src/overlay/FlowHUDTemplates.js`):**
  - Updated default model for Image modes (`text-to-image`, `edit-image`) to `Nano Banana 2` (replacing Nano Banana Pro).
  - Updated default model for Video modes (`text-to-video`, `image-to-video`, `frames-to-video`) to `Veo 3.1 - Lite` (replacing Omni 1.1 Flash).
  - Updated `DEFAULT_CONFIG.model`, `enqueueItem`, `enqueueBatch`, and `FlowHUDTemplates.js` `#selModelFamily` optgroup default selections.
- **Issue 2: Strict Mode/Model Normalization & Popover Mode Fix (`src/core/FlowStorage.js`, `src/services/FlowSettingsService.js`, `src/core/QueueManager.js`, `src/overlay/FlowHUDHost.js`):**
  - Implemented and exported `getDefaultModelForMode(mode)` and `normalizeModelForMode(mode, currentModel)` in `FlowStorage.js`.
  - Enforced strict compatibility: Image modes (`t2i`, `ei`) strictly normalize to Image Models (`Nano Banana 2`), while Video modes (`t2v`, `i2v`, `f2v`) strictly normalize to Video Models (`Veo 3.1 - Lite`).
  - Fixed popover mode mutation bug in `FlowSettingsService.applySettings()`: eliminated aggressive auto-align logic that previously mutated `edit-image` to `text-to-video` when a video model was supplied. Mode is now strictly preserved and model is normalized to the appropriate model family.
  - Enforced `normalizeModelForMode` across `QueueManager.js` and `FlowHUDHost.js` (`addQueueRow`, `handleBulkFiles`, `saveCurrentQueue`, `convertQueueBetweenModes`, `syncSidebarControlsToItem`, and `syncModeUI`).
- **Issue 3: Sequential Frame-to-Video Paste Ingestion (`src/services/FlowIngredientService.js`, `src/core/QueueManager.js`):**
  - Resolved 30000ms timeout failure during `frames-to-video` frame injection.
  - Abandoned targeting non-input DOM elements (`div.frame-trigger`); now dispatches native clipboard paste events sequentially into Google Flow's ProseMirror editor, matching `i2v`/`ei` mechanics.
  - In `frames-to-video` mode:
    1. 1st paste injection into ProseMirror editor automatically mounts Start Frame (`expectedChipCount = 1`).
    2. Wait for Start Frame chip upload completion (`aria-busy="false"`, overlay removed, valid `chip-image`).
    3. 2nd paste injection into ProseMirror editor automatically mounts End Frame (`expectedChipCount = 2`).
    4. Wait for End Frame chip upload completion with 500ms settling delay before prompt injection.
- **Issue 4: Thumbnail Persistence Hydration on Idle/Stop (`src/overlay/FlowHUDHost.js`):**
  - Resolved bug where queue row thumbnails disappeared into `+ Drop` and empty `Start`/`End` placeholders when the queue transitioned to IDLE or STOPPED.
  - Added `await this.hydrateQueuePreviews()` inside `syncQueueFromStorage()` to reconstitute Base64 data URLs from `flowImageDB` IndexedDB before invoking `renderQueueContent()`.
- **Verification:**
  - Automated unit test suite `test_model_and_frame_fixes.mjs` executed and passed 100% (verifying defaults for all 5 modes, normalization of mismatched models, and preservation of valid model choices).
  - All 18 JavaScript files in `src/` verified syntax-valid via `node --check` (0 errors).
  - Strict Zero Native Emoji compliance verified across all code, templates, and documentation.

---

## Session Entry 3 — Smart Image Drop, Ingredient Click-to-Swap in Sort Mode, Prompt Hygiene, Start Validation, and Right-Aligned Status Badge

**Time:** ~10:25 WIB  
**Branch:** `task/dual-mode-ui`  
**Commit:** `9ffec65` (`fix(hud): smart image drop ingredient swap in sort mode prompt validation and status badge alignment`)  
**Product code changed:** YES (`src/overlay/FlowHUDHost.js`, `src/overlay/FlowHUDTemplates.js`, `src/overlay/overlay.css`)  
**Pushed:** NO  

### What Was Done
- **Issue 1 & 3: Smart Image Drop & Prompt Hygiene (`src/overlay/FlowHUDHost.js`):**
  - Refactored `handleBulkFiles()` to isolate image files and process them concurrently.
  - Resolved empty dropzone bug where images were ingested into `ingredients` while mode was `frames-to-video`, leaving frame slots empty.
  - Implemented smart mode resolution on drop:
    - 1 image: automatically switches to `image-to-video` (or `edit-image` if previously selected) and creates 1 row with `ingredients: [img]`.
    - 2 images: automatically switches to `frames-to-video` and creates 1 row with `frames: { start: img1, end: img2 }`.
    - >2 images: creates N rows with 1 ingredient each in `image-to-video` (or `edit-image`).
  - Purged automatic filename injection into prompt textarea. All dropped images initialize with pure empty prompt (`prompt: ''`), requiring explicit prompt entry.
  - Implemented smart queue conversion on mode change (`convertQueueBetweenModes()`): switching to `frames-to-video` pairs ingredients 2-by-2 into Start & End frames; switching to single-image modes unpairs them into individual rows.
- **Issue 2: Universal Ingredient Click-to-Swap in Sort Mode (`src/overlay/FlowHUDHost.js`, `src/overlay/FlowHUDTemplates.js`, `src/overlay/overlay.css`):**
  - In Sort Mode (`isSortMode = true`), clicking `.row-media-slot` suppresses native file picker dialogs across all modes (`frames-to-video`, `image-to-video`, `edit-image`).
  - Implemented click-to-swap: first click marks slot as swap source (`.swap-source` with pulsing cyan glow); second click on another slot swaps media payloads between rows. Clicking same slot deselects cleanly.
  - Added dynamic tooltips in `renderQueueRow` indicating swap capability when sort mode is active.
- **Issue 4: Mandatory Prompt Validation for Start Button (`src/overlay/FlowHUDHost.js`, `src/overlay/overlay.css`):**
  - Added `updateStartButtonState()`: verifies `queueItems.length > 0` and every row has a non-empty trimmed prompt (`prompt.trim().length > 0`).
  - If any prompt is empty, `#btnStartQueue` is disabled (`opacity: 0.42`, `cursor: not-allowed`) with tooltip *"Enter prompt for all rows to start generation"*.
  - Start button dynamically updates in real-time on textarea `input`, row add, row delete, drop, and queue state transitions.
- **Issue 5: Right-Aligned Row Status Badge (`src/overlay/overlay.css`):**
  - Changed `.row-status-badge` from `align-self: flex-start;` to `align-self: flex-end;`, positioning badges flush to the bottom-right of the prompt textarea.
- **Issue 6: Auto-Save Hardening (`src/overlay/FlowHUDHost.js`):**
  - Added 500ms debounced auto-save on prompt textarea `input` and immediate save on `blur`. All settings, parameters, queue rows, and prompt texts are now 100% persisted to storage in real-time, making manual save redundant.
- **Verification:**
  - Automated test suite `test_drop_and_sorting_logic.mjs` executed and passed 100% (covering 1-image drop, 2-image drop, 10-image drop, 2-by-2 pairing/unpairing conversion, cross-row ingredient swap across f2v/i2v, and start button prompt validation).
  - Clean syntax check across all 18 JS modules in `src/` (`node --check`).
  - Zero Native Emoji policy strictly preserved across all code, UI templates, and documentation.

---

## Session Entry 2 — Container Focus Toggle, Multi-Select Transition, Uncheck Focus Clearing, and Active-Row Bulk Deletion

**Time:** ~02:00 WIB  
**Branch:** `task/dual-mode-ui`  
**Commit:** `19064d3` (`fix(hud): selection focus toggle multi-select transition and uncheck focus clearing`)  
**Product code changed:** YES (`src/overlay/FlowHUDHost.js`)  
**Pushed:** NO  

### What Was Done
- **Issue 1: Focus Toggle on Row Container Click (Kondisi Pertama, `src/overlay/FlowHUDHost.js`):**
  - Refactored single container click logic: when 0 rows are checked and a row is clicked, it becomes focused (`this.activeRowIdx = idx`, `.row-active` cyan accent) without checking the checkbox.
  - Clicking the same row container a second time toggles off focus (`this.activeRowIdx = null`), completely removing `.row-active` and returning the row to neutral un-focused state.
- **Issue 2: Transition from Single Focus to Multi-Select (Kondisi Kedua, `src/overlay/FlowHUDHost.js`):**
  - Normal click when 0 rows are checked switches active focus between rows cleanly without checking any checkboxes.
  - When row A (e.g. row 2) is focused without checkmark, `Ctrl` / `Cmd` + clicking container row B (e.g. row 3) now checks both row A and row B (`selected = true`). Seamlessly transitions into multi-selection mode using standard desktop modifier keys without requiring precise checkbox clicks.
- **Issue 3: Unchecking Focus Clearing & Active Row Shift (`src/overlay/FlowHUDHost.js`):**
  - Fixed bug where unchecking row 2 left it in active focus state (`.row-active` cyan border) shown in user screenshot.
  - When unchecking a row (either via direct checkbox click or clicking container of an already checked row):
    - The row loses both checkmark (`selected = false`) and focus.
    - If another checked row remains (e.g. row 3), active focus shifts to that remaining checked row (`this.activeRowIdx = remaining`).
    - If no checked rows remain, `this.activeRowIdx` resets to `null`, completely clearing `.row-active` across all rows, hiding the trash icon, and reverting the Single mode sidebar to `#sidebarSinglePlaceholder`.
- **Issue 4: Active Row Trash Button Visibility & Deletion (`src/overlay/FlowHUDHost.js`):**
  - In `updateSelectionUI()`, set `#btnBulkDeleteQueue` visibility to `hasSelectionOrActive = selectedCount > 0 || (this.activeRowIdx !== null && Boolean(this.queueItems[this.activeRowIdx])) ? 'inline-flex' : 'none'`.
  - In `#btnBulkDeleteQueue` click handler, if `selectedCount === 0` but a single row is active/focused via container click, clicking the trash button deletes that active row directly and resets `activeRowIdx = null`.
  - When no rows are checked and no rows are focused, the trash button is strictly hidden.
- **Verification:**
  - Automated test script `test_hud_selection_and_banner.mjs` updated covering Kondisi 1, Kondisi 2, container uncheck, and active row bulk deletion. All test suites passed 100%.
  - Clean syntax check across all modified files (`node --check`).
  - Zero Native Emoji policy strictly preserved across all code, UI templates, and documentation.

---

## Session Entry 1 — Container Selection Refinement, Strict Conditional Trash Icon Hide, and Queue Prompt Textarea Default Height Expansion

**Time:** ~01:05 WIB  
**Branch:** `task/dual-mode-ui`  
**Commit:** `78c55e2` (`fix(hud): row container selection behavior conditional trash visibility and prompt textarea height`)  
**Product code changed:** YES (`src/overlay/FlowHUDHost.js`, `src/overlay/FlowHUDTemplates.js`, `src/overlay/overlay.css`)  
**Pushed:** NO  

### What Was Done
- **Issue 1: Row Container Click vs Checkbox Click Multi-Select Logic (`src/overlay/FlowHUDHost.js`):**
  - Refactored row container click handler in `bindRowEvents()`:
    - **When 0 items are checked (`selectedCount === 0`)**: Clicking a row container focuses/activates that row (`this.activeRowIdx = idx`, applying `.row-active` cyan accent highlight) **without checking its checkbox** (`it.selected = false`). In Single Mode, sidebar controls display parameters for this active row (`ROW #X`) with clamped prompt preview, enabling instant parameter inspection and editing without checking any checkboxes. Checkboxes remain strictly unchecked across both Batch and Single modes.
    - **When 1 or more items are already checked (`selectedCount > 0`)**: Clicking any other row container automatically checks that row too (`it.selected = true`, "ikut kecentang"), adding it to the multi-selection set without requiring the user to aim precisely at the 16px checkbox.
    - **Direct Checkbox Click**: Clicking the `.row-select-checkbox` directly (`change` event with `e.stopPropagation()`) continues to toggle individual row selection independently without deselecting other checked rows.
    - **Prompt Input Focus**: Clicking or focusing inside `.row-prompt-input` sets `activeRowIdx = idx` and triggers `updateSelectionUI()` without toggling checkbox state.
    - **Parameter Modifications**: In Single mode, changes to mode, model, duration, aspect ratio, outputs, and resolution apply to all checked rows (`selected.length > 0`) or fall back to the active focused row (`this.activeRowIdx !== null`).
- **Issue 2: Strict Conditional Trash Icon Hide (`src/overlay/FlowHUDHost.js`):**
  - Fixed regression where `#btnBulkDeleteQueue` remained visible in Single mode when no rows were checked (previously caused by `(this.paramMode === 'single' && this.activeRowIdx !== null)`).
  - In `updateSelectionUI()`, strictly enforced `btnBulkDelete.style.display = selectedCount > 0 ? 'inline-flex' : 'none';`. The trash button is now completely hidden whenever 0 rows are checked, in both Single and Batch modes.
  - In `#btnBulkDeleteQueue` click handler, strictly enforced `toDelete.length === 0 return;` targeting only items where `it.selected === true`.
- **Issue 3: Queue Row Height & Textarea Default Height Expansion (`src/overlay/FlowHUDTemplates.js`, `src/overlay/overlay.css`):**
  - In `renderQueueRow()` template, increased default textarea rows from `rows="1"` to `rows="2"`.
  - In `overlay.css`:
    - Refactored `.hud-queue-row` to `padding: 10px; align-items: flex-start; gap: 10px; box-sizing: border-box;`.
    - Added `margin-top: 6px;` to `.row-select-handle` for dead-center vertical alignment with the first line of the expanded textarea.
    - Added `.hud-queue-row.row-checked` alongside `.row-active` to visually differentiate checked rows.
    - Refactored `.row-prompt-input`:
      - Increased `min-height` from 30px to `54px` and `max-height` to `130px`.
      - Set `font-size: 13px; line-height: 1.45; padding: 7px 9px; box-sizing: border-box;`.
      - Displays 2 to 3 lines of prompt text simultaneously by default without requiring manual textarea dragging.
- **Verification:**
  - Updated unit test script `test_hud_selection_and_banner.mjs` verifying:
    - Container click with 0 checked rows leaves checkbox unchecked and trash button hidden.
    - Active row inspection in Single Mode displays `ROW #2` with trash button hidden.
    - Checkbox click activates checkbox and reveals trash button.
    - Subsequent container click with 1+ checked rows adds target row to multi-selection ("ikut kecentang") and maintains trash button visibility.
    - Textarea template renders with `rows="2"` and resolution labels match specification.
  - All test suites passed 100%.
  - Syntax checked clean via `node --check src/overlay/FlowHUDHost.js src/overlay/FlowHUDTemplates.js src/overlay/CustomSelect.js`.
  - Zero Native Emoji policy strictly preserved across all code, UI templates, and documentation.
